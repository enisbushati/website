spring:
  profiles:
    active: local

  datasource:
    url: jdbc:mysql://localhost:3306/webshop?serverTimezone=UTC
    username: 
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true

web:
  resources:
    static-locations: file:./webshopapi/uploads/

mvc:
  static-path-pattern: /uploads/**

app:
  upload-dir: ./webshopapi/uploads

